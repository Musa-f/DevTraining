{% extends 'base.html.twig' %}

{% block body %}

<div class="card" style="width: 18rem;">
  <img src="{{ asset('images/horloge.jpg') }}" class="card-img-top" alt="...">
  <div class="card-body">
    <h4 class="card-title">On est le: {{affichageDate}}</h4>
    <h5 class="card-title">Il est : {{affichageHeure}}</h5>
  </div>
</div>

{% endblock %}