{% extends 'base.html.twig' %}

{% block title %}Author{% endblock %}

{% block body %}
    <h1>Author</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ author.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ author.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ author.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_author_crud_index') }}">back to list</a>

    <a href="{{ path('app_author_crud_edit', {'id': author.id}) }}">edit</a>

    {{ include('author_crud/_delete_form.html.twig') }}
{% endblock %}
